# LangGraph Deployment URL (Required)
# For local development, use: http://127.0.0.1:2024
# This is the URL where your backend LangGraph server is running
NEXT_PUBLIC_DEPLOYMENT_URL=http://127.0.0.1:2024

# Agent ID from langgraph.json (Required)
# This is the key under "graphs" in the langgraph.json file
# In the default setup, this is "research"
NEXT_PUBLIC_AGENT_ID=research

# Authentication (Optional)
# Set to true if you want to enable authentication
# For beginners, it's recommended to keep this as false until you're familiar with the system
NEXT_PUBLIC_AUTH_ENABLED=false
